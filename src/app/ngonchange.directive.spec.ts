import { NgonchangeDirective } from './ngonchange.directive';

describe('NgonchangeDirective', () => {
  it('should create an instance', () => {
    const directive = new NgonchangeDirective();
    expect(directive).toBeTruthy();
  });
});
